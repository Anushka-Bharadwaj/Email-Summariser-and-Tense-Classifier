# Email Summariser & Tense Classifier

An AI-powered tool that automatically summarizes long emails and classifies the grammatical tense being used.

## Setup
Install the dependencies:
```bash
pip install -r requirements.txt
